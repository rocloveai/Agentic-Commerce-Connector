{
  "name": "@acc/install-relay",
  "version": "0.8.1",
  "private": true,
  "description": "Shared-app OAuth relay for Agentic Commerce Connector. Accepts Shopify OAuth callbacks on behalf of all merchants running ACC; relays tokens to their CLI via short-lived pair codes.",
  "type": "module",
  "main": "./build/server.js",
  "bin": {
    "acc-install-relay": "./build/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@acc/connector": "^0.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "vitest": "^3.1.1"
  }
}
