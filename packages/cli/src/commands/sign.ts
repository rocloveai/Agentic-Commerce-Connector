export async function runSign(_args: string[]): Promise<void> {
  throw new Error(
    "acc-skill sign: not implemented yet. Planned: load manifest + tools + openapi, compute content_hash, EIP-712 sign.",
  );
}
