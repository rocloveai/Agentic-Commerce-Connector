export async function runVerify(_args: string[]): Promise<void> {
  throw new Error(
    "acc-skill verify: not implemented yet. Planned: validate schemas, recompute content_hash, recover signer, compare to merchant_address.",
  );
}
