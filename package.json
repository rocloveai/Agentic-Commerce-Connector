{
  "name": "agentic-commerce-connector",
  "version": "0.8.1",
  "private": true,
  "description": "Open-source UCP/1.0 data-layer wrapper for traditional e-commerce + agent-facing skill toolchain",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/skill-spec",
    "packages/connector",
    "packages/cli",
    "packages/install-relay"
  ],
  "scripts": {
    "build": "npm run build -w @acc/skill-spec && npm run build -w @acc/connector && npm run build -w @acc/cli && npm run build -w @acc/install-relay",
    "build:binary": "bun run scripts/build-binary.ts",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspace packages/connector",
    "start": "npm run start --workspace packages/connector"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "vitest": "^3.1.1"
  }
}
